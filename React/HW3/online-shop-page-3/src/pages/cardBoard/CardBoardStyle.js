import { createUseStyles } from "react-jss";

const cardBoardStyle = createUseStyles({
  cardBoard: {
    display: "flex",
    justifyContent: "space-between",
    flexWrap: "wrap",
    padding: "0 15px"
  }
});

export default cardBoardStyle;
